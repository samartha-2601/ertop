version: '3.9'

services:
  postgres:
    image: postgres:16
    container_name: ertop-postgres
    restart: always
    environment:
      POSTGRES_USER: ertop
      POSTGRES_PASSWORD: ertoppassword
      POSTGRES_DB: ertopdb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: ertop-redis
    restart: always
    ports:
      - "6379:6379"

volumes:
  postgres_data: